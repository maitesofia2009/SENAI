let idades = [5,12,16,18,25,54]
const primeironumero = idades[0];
const ultimonumero = idades[idades.length - 1];
const soma = primeironumero + ultimonumero;
console.log(`Soma do primeiro (${primeironumero}) com o último (${ultimonumero}) elemento: ${soma}`);