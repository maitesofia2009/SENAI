let pessoa = {
    nome:"maite",
    idade: 16,
    altura: 1.55,
    estudante: true
}
 console.log (`nome: ${pessoa.nome} -idade: ${pessoa.idade} -altura: ${pessoa.altura} -estudante: ${pessoa.estudante}`)