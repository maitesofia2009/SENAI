let pessoa = {
    nome:"maite",
    idade: 16,
    altura: 1.55,
    estudante: true
}
 console.log (`meu nome é ${pessoa.nome}, tenho ${pessoa.idade} anos, ${pessoa.altura} de altura, e sou estudante: ${pessoa.estudante}`)